10 DIM B(9):REM BOARD (0=EMPTY, 1=X, 2=O)
20 FOR I=1 TO 9:B(I)=0:NEXT:REM INIT BOARD
30 GOSUB 100:REM PLAYER MOVE
40 GOSUB 200:REM AI MOVE
50 GOTO 30
100 REM PLAYER MOVE (SIMPLE INPUT)
110 INPUT "YOUR MOVE (1-9)";M
120 IF B(M)<>0 THEN PRINT "TAKEN!":GOTO 110
130 B(M)=1:RETURN
200 REM AI MOVE (BASIC MINIMAX)
210 FOR I=1 TO 9
220 IF B(I)=0 THEN GOSUB 300:REM SCORE MOVE
230 NEXT
240 REM PICK BEST MOVE (SIMPLIFIED)
250 RETURN
300 REM SCORE POSITION (STUB)
310 S=0:REM EVALUATE BOARD
320 RETURN
